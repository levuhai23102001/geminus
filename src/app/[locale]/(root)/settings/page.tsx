import { SettingFC } from "@/app/components/Settings";
import React from "react";

type Props = {};

const Settings = (props: Props) => {
    return (
        <>
            <SettingFC />
        </>
    );
};

export default Settings;
