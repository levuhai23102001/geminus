import { CardPhotoList } from "@/app/components/Card/CardPhoto";

type Props = {};

const Explore = (props: Props) => {
    return (
        <div className="py-5">
            <CardPhotoList />
        </div>
    );
};

export default Explore;
