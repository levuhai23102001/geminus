import { Profile as ProfileFC } from "../../../../components/Profile";

type Props = {};

const Profile = (props: Props) => {
    return (
        <div className="p-9">
            <ProfileFC />
        </div>
    );
};

export default Profile;
